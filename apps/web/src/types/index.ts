export type Role = 'student' | 'recruiter';
export type Notice = { type: 'error' | 'success' | 'info'; text: string } | null;
export type AuthUser = { id: string; email: string; roles?: Role[] };
export type AuthPayload = { accessToken: string; refreshToken?: string; user?: AuthUser };
export type WorkMode = 'remote' | 'hybrid' | 'onsite';
export type ApplicationStatus = 'saved' | 'applied' | 'assessment' | 'interview' | 'offer' | 'rejected' | 'joined';
export type Internship = { _id: string; title: string; company: string; description: string; requirements?: string[]; skills?: string[]; location?: string; workMode?: WorkMode; stipendMin?: number; stipendMax?: number; currency?: string; source?: string; sourceUrl?: string; difficultyScore?: number; isActive?: boolean; matchScore?: number; missingSkills?: string[]; deadline?: string; createdAt?: string; updatedAt?: string };
export type Application = { _id: string; internshipId?: string; title: string; company: string; status: ApplicationStatus; notes?: string; sourceUrl?: string; updatedAt?: string; createdAt?: string };
export type Profile = { _id?: string; fullName?: string; phone?: string; location?: string; bio?: string; completenessScore?: number; education?: { institution: string; degree?: string; fieldOfStudy?: string; graduationYear?: number }[]; skills?: { name: string; proficiencyLevel: number; source: 'manual' | 'resume_extracted' }[]; preferences?: { roles: string[]; locations: string[]; workModes: WorkMode[]; minStipend?: number } };
export type Resume = { _id: string; originalName: string; status: string; processedAt?: string; analysis?: { resumeScore?: number; skillMatrix?: { name: string }[]; recommendations?: string[] } };
