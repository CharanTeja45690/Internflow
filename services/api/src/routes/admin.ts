import { Router } from 'express'; import { requireAuth } from '../middleware/auth'; import { User } from '../models/User'; import { Internship } from '../models/Internship'; import { Application } from '../models/Application'; import { Resume } from '../models/Resume'; import { asyncHandler } from '../utils/asyncHandler';
export const adminRouter=Router(); adminRouter.use(requireAuth); adminRouter.use((req,res,next)=>req.user!.role==='admin'?next():res.status(403).json({message:'Admin role required'}));
adminRouter.get('/overview', asyncHandler(async(_req,res)=>{ const [users,students,recruiters,internships,applications,resumes]=await Promise.all([User.countDocuments(),User.countDocuments({roles:'student'}),User.countDocuments({roles:'recruiter'}),Internship.countDocuments(),Application.countDocuments(),Resume.countDocuments()]); res.json({users,students,recruiters,internships,applications,resumes}); }));
adminRouter.patch('/users/:id/status', asyncHandler(async(req,res)=>{ const status=['active','suspended','deleted'].includes(req.body.status)?req.body.status:null; if(!status) return res.status(400).json({message:'Invalid status'}); const user=await User.findByIdAndUpdate(req.params.id,{status},{new:true}).select('-passwordHash -sessions'); if(!user) return res.status(404).json({message:'User not found'}); res.json(user); }));
