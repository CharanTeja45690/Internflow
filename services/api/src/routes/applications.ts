import { Router } from 'express'; import { applicationSchema, applicationStatuses } from '@internflow/shared'; import { requireAuth } from '../middleware/auth'; import { Application } from '../models/Application'; import { asyncHandler } from '../utils/asyncHandler';
export const applicationRouter=Router(); applicationRouter.use(requireAuth);
applicationRouter.get('/', asyncHandler(async(req,res)=>res.json(await Application.find({userId:req.user!.id}).sort({updatedAt:-1}))));
applicationRouter.post('/', asyncHandler(async(req,res)=>{ const input=applicationSchema.parse(req.body); const existing=input.internshipId ? await Application.findOne({userId:req.user!.id,internshipId:input.internshipId}) : null; if(existing){ const from=existing.status; existing.set({...input,status:input.status}); if(from!==input.status) existing.statusHistory.push({from,to:input.status,changedAt:new Date()} as any); await existing.save(); return res.status(200).json(existing); } const doc=await Application.create({...input,userId:req.user!.id,statusHistory:[{to:input.status,changedAt:new Date()}]}); res.status(201).json(doc); }));
applicationRouter.patch('/:id/status', asyncHandler(async(req,res)=>{ const status=applicationStatuses.includes(req.body.status) ? req.body.status : null; if(!status) return res.status(400).json({message:'Invalid application status'}); const app=await Application.findOne({_id:req.params.id,userId:req.user!.id}); if(!app) return res.status(404).json({message:'Application not found'}); const from=app.status; app.status=status; if(from!==status) app.statusHistory.push({from,to:status,changedAt:new Date()} as any); await app.save(); res.json(app); }));
applicationRouter.patch('/:id', asyncHandler(async(req,res)=>{ const input=applicationSchema.partial().parse(req.body); const doc=await Application.findOneAndUpdate({_id:req.params.id,userId:req.user!.id},input,{new:true}); if(!doc) return res.status(404).json({message:'Application not found'}); res.json(doc); }));
