import express from 'express'; import cors from 'cors'; import helmet from 'helmet'; import compression from 'compression'; import morgan from 'morgan'; import rateLimit from 'express-rate-limit'; import { env } from './config/env'; import { authRouter } from './routes/auth'; import { profileRouter } from './routes/profile'; import { internshipRouter } from './routes/internships'; import { applicationRouter } from './routes/applications'; import { resumeRouter } from './routes/resumes'; import { recommendationRouter } from './routes/recommendations'; import { healthRouter } from './routes/health'; import { errorHandler, notFound } from './middleware/error';
export const app=express(); app.use(helmet()); app.use(cors({origin:env.CORS_ORIGIN,credentials:true})); app.use(compression()); app.use(express.json({limit:'1mb'})); app.use(morgan('tiny')); app.use(rateLimit({windowMs:15*60*1000,limit:300})); app.use('/health',healthRouter); app.use('/api/auth',authRouter); app.use('/api/profile',profileRouter); app.use('/api/internships',internshipRouter); app.use('/api/applications',applicationRouter); app.use('/api/resumes',resumeRouter); app.use('/api/recommendations',recommendationRouter); app.use(notFound); app.use(errorHandler);
