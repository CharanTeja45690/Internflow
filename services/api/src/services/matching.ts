export function matchScore(profileSkills: string[] = [], roleSkills: string[] = []) { if (!roleSkills.length) return 50; const set = new Set(profileSkills.map(s=>s.toLowerCase())); const matches = roleSkills.filter(s=>set.has(s.toLowerCase())).length; return Math.min(99, Math.round((matches / roleSkills.length) * 70 + 25)); }
export function analyzeResumeText(text: string) { const known = ['javascript','typescript','react','node.js','express','mongodb','python','sql','aws','docker','git','html','css']; const lower = text.toLowerCase(); const skills = known.filter(s=>lower.includes(s)); const resumeScore = Math.min(95, 45 + skills.length * 5 + (text.length > 1200 ? 10 : 0)); return { resumeScore, skillMatrix: skills.map(name=>({ name, confidence: 0.82 })), atsIssues: text.length < 800 ? ['Resume appears short; add measurable project and internship impact.'] : [], recommendations: ['Quantify outcomes with metrics.', 'Align top skills with target internship requirements.', 'Add links to GitHub, portfolio, and deployed projects.'] }; }
