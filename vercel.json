{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "ROOT=$(git rev-parse --show-toplevel) && npm --prefix $ROOT run build:shared && npm --prefix $ROOT run build:web",
  "outputDirectory": "dist",
  "installCommand": "ROOT=$(git rev-parse --show-toplevel) && npm --prefix $ROOT install",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
